server:
  port: 8802

eureka:
  client:
    serviceUrl:
      defaultZone: http://woo-eureka:8761/eureka/

spring:
  application:
    name: hystrix-dashboard

turbine:
  aggregator:
    cluster-config: default
  cluster-name-expression: new String("default")
  app-config: mall-order